{
  "name": "cypresstester",
  "version": "1.0.0",
  "description": "Cypress Tester",
  "repository": "https://github.com/atristell/cypress-jenkins-ci",
  "scripts": {
    "test": "$(npm bin)/cypress open --project ./tareas --env configFile=development",
    "test:ci": "$(npm bin)/cypress run --project ./tareas --env configFile=development"
  },
  "author": "agarciat",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^3.1.3"
  }
}
